info:
  name: create-cart-items
  type: http
  seq: 1

http:
  method: POST
  url: "{{base_url}}/api/cart/items"
  body:
    type: json
    data: |-
      {
        "productId": 4,
        "quantity": 1
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
​